@model Fidly.ViewModels.NewMovieViewModel

@{
    ViewBag.Title = "MovieForm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using(Html.BeginForm("Save", "Movies"))
{
    <h2>Movie Form</h2>

    <div class="form-group">
        <div class="bottom-one">
        @Html.LabelFor(m => m.Movie.Name)
        @Html.TextBoxFor(m => m.Movie.Name, new { @class="form-control"})
        </div>
    </div>

    <div class="form-group">
        <div class="bottom-one">
        @Html.LabelFor(m => m.Movie.GenreId)
        @Html.DropDownListFor(m => m.Movie.GenreId, new SelectList(Model.Genre, "Id", "GenreName"), "Select Genre", new { @class="form-control" })
        </div>
    </div>

    <div class="form-group">
        <div class="bottom-one">
        @Html.LabelFor(m => m.Movie.ReleaseDate)
        @Html.TextBoxFor(m => m.Movie.ReleaseDate, "{0:d MMM yyyy}",  new { @class="form-control"})
        </div>
    </div>

    <div class="form-group">
        <div class="bottom-one">
        @Html.LabelFor(m => m.Movie.NumberInStock)
        @Html.TextBoxFor(m => m.Movie.NumberInStock,  new { @class="form-control"})
        </div>
    </div>
    
    @Html.HiddenFor(m => m.Movie.Id)

    <button class="btn btn-primary" type = "submit">Save</button>    
}
<section class="bg-light">
    <div class="container">
        <div class="text-center"
            <h2> Stay in the loop</h2>
            <p class="lead">Get the latest...</p>
    </div>
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <p class="text-muted my-4">some text</p> 
            <button class="btn  btn-primary" dta-bs-toggle="modal"
            data-bs-target="#reg-modal">Register
            </button>
        </div>
    </div>
 </section>
